To add user:
	git config --global user.name "Your Name"

To add email:
	git config --global user.email "Your email_id"


To check whether they are added successfully,
	git config --list

You can check only specific info
	git config user.email
